version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_ml_challenge
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: always

volumes:
  mongo-data: